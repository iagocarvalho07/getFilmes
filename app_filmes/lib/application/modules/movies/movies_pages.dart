import 'package:flutter/material.dart';
    
class MoviesPages extends StatelessWidget {

  const MoviesPages({ Key? key }) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(child: const Text('moviesPage') ,),
    );
  }
}